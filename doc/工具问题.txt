### 1、连接一个未写入SN号的设备，下发AT^DMSN，工具异常退出
     
2、连接功能机工具下发ATD拨号指令，设备端无拨号界面

### 3、更改工具的名称

### 4、连接Android设备输入号码拨号，设备端要拨打的号码数字是一个个跳出而不是一次性显示

5、输入号码和短信内容，点击发送只是跳转到短信发送界面，再次点击工具端发送设备端调到文本模板界面

### 6、Android界面，接收区右侧加一个清除

### 7、连接1606设备，点击WiFi信息返回的是WiFi未连接

### 8、插入SIM卡和SD卡，点击查询SIM卡和SD卡状态，将查询的结果改为：
	Sim卡状态：正常/异常
	SD卡状态：正常/异常

### 9、输入对端号码，点击拨号工具上方显示无响应

10.短信输入中文符号时有闪退，unicode问题

11.
添加压力测试项
    - 失败时是否继续执行
主叫接听
    1.语音业务，需要连接两台设备
    2.使用ATDxxxx;拨打号码，定时接收，清空一个接收AT返回的列表，返回目前的通话状态
主叫主挂
    1.语音业务，连接测试设备
    2.使用ATDxxx;拨打，设置响铃时间，时间到后使用CHUP挂断，等待n秒后再次执行
主叫被挂
        1.语音业务，连接两台设备
    2.使用ATDxxx;拨打，设置响铃时间，
主叫未接
被叫接听
被叫挂断

短信发送
短信接收

待机入网检测